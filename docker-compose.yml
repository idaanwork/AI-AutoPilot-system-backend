version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: autopilot
    ports:
      - "43292:43292"
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  api:
    build: .
    command: npm run dev
    environment:
      DATABASE_URL: postgresql://postgres:oMuZmcPyOPXdhbolSzYbvWorfHnyrKvm@hopper.proxy.rlwy.net:43292/railway?sslmode=require
      REDIS_URL: redis://redis:6379
      OPENAI_KEY: ""
      WHATSAPP_TOKEN: ""
    depends_on:
      - db
      - redis
    ports:
      - "8080:8080"

volumes:
  db_data:
